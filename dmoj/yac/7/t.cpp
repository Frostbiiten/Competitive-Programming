#include <iostream>

int main()
{
    std::ios_base::sync_with_stdio(0); std::cin.tie(0); std::cout.tie(0);

    std::sort(a, a + n, [](p a, p b)
    {
        int aa = a.first >> 1;
        int bb = b.first >> 1;
        if (aa == bb)
        {
            return 
        }
    });
}